# Dependencias para ejecutar los notebooks y regenerar los reportes HTML.
# Probado con Python 3.11.

# Cálculo y datos
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1          # lectura de los libros Excel de entrada

# Ejecución de notebooks
jupyter>=1.0
nbconvert>=7.0

# Generación del reporte HTML offline (fórmulas en MathML, sin CDN)
latex2mathml>=3.7
beautifulsoup4>=4.11
